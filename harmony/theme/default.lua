return {
    inputBackground = colors.green,
    inputForeground = colors.white,
    foreGround = colors.white,
    background = colors.black,
    decoration = colors.cyan,
    errorColor = colors.red,
    warningColor = colors.yellow,
    successColor = colors.green,
    listBackground = colors.gray,
    barLabel = colors.gray
}
